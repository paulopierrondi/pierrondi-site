# whypaulo - proof + language TODO

Status in canonical repo (`/Users/paulopierrondi/Projects/pierrondi-site`): partially done on 2026-05-29.

## Done

- Repositioned `/whypaulo` to lead with the hardest-to-copy asset: ServiceNow + governed AI + FSI Brazil operating context.
- Removed invented proof numbers from `theaterStats`.
- Kept only defensible proof framings:
  - `FSI` / ServiceNow at top-tier Brazilian bank scale.
  - `BACEN/LGPD` / governed AI under real regulation.
  - `CMDB + Now Assist` / platform depth, not slideware.
- Added PT-BR primary copy.
- Added EN switch inside the experience.
- Added a first-90-days plan section.
- Preserved `robots: noindex`.
- Removed the token gate after Paulo's explicit command on 2026-05-29 (`remove o token, nao quero mais`).
- Deleted the `/api/whypaulo/session` route and the `lib/whypaulo/auth.ts` helper.
- `/whypaulo` is now directly reachable by URL, while still `robots: noindex`.

## Still Needs Paulo

Add exactly one real, anonymized measured outcome as a 4th metric in `WhyPauloExperience.tsx`.

Use a number you would defend live to a ServiceNow SVP, for example:

- `Nx` faster governed AI POV cycle.
- `N%` adoption or MAU lift on an anonymized program.
- `N agents` governed in production under audit.
- `N days` from executive problem to governed working proof.

Do not add a number unless it is real, anonymizable and defensible.

## Voice Pass

Before sharing externally, Paulo should re-read:

- Hero lead.
- First 90 days section.
- Author / ask section.

The current copy is intentionally assertive. It is suitable for a targeted executive hiring pitch, but the final voice pass should confirm how direct Paulo wants the ask to be.
